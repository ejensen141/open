#include "read_map.h"

#include<iostream>

const char* getprogram(char *file){
int N = 100;
const char* program;
program = "hello";
char *extension;
int index = -1;
//std::cout<<file<<std::endl;
	

	index = (int)(strchr(file,'.')-file);

	std::cout<<index<<"	"<<sizeof(file)<<std::endl;
	if(index>0 && index <1000)
	{
	extension = strrchr(file,'.');
	
	}
	else
	{
	 return "/usr/bin/gedit";
	}	
	
	



/*
char ***map = new char**[N];

for(int i = 0; i<N; i++)
{
*map = new char*[N]; 
}

*/

	


	if(strcmp (extension,".pdf") == 0 )
	
	{	//std::cout<<extension<<std::endl;
		program = "/usr/bin/evince";
	
	}
	
	if(strcmp (extension,".c") == 0 )
	
	{	//std::cout<<extension<<std::endl;
		program = "/usr/bin/gedit";
	
	}


	if(strcmp (extension,".txt") == 0 )
	
	{	//std::cout<<extension<<std::endl;
		program = "/usr/bin/gedit";
	
	}




return program;
}


